# XcodeGen spec — regenerates ViciInbox.xcodeproj deterministically.
#   brew install xcodegen && cd ios && xcodegen generate
# The generated .xcodeproj is committed for cloud CI; this file remains the
# human-readable source of truth.

name: ViciInbox
options:
  bundleIdPrefix: com.vicipeptides
  # iOS 16 baseline: LabeledContent and modern SwiftUI APIs used in Settings.
  deploymentTarget:
    iOS: "16.0"
  createIntermediateGroups: true
  groupSortPosition: top

settings:
  base:
    MARKETING_VERSION: "1.0.0"
    CURRENT_PROJECT_VERSION: "1"
    SWIFT_VERSION: "5.9"
    IPHONEOS_DEPLOYMENT_TARGET: "16.0"
    TARGETED_DEVICE_FAMILY: "1"     # iPhone only
    ENABLE_BITCODE: NO
    CODE_SIGN_STYLE: Automatic
    # Team ID read from the issued VoIP certificate (OU field).
    DEVELOPMENT_TEAM: PQFYN2CD77

packages:
  TelnyxRTC:
    url: https://github.com/team-telnyx/telnyx-webrtc-ios
    # The native call code was audited against this exact API. Do not allow a
    # later 4.x release to change underneath an uncompiled release build.
    exactVersion: "4.1.2"

targets:
  ViciInbox:
    type: application
    platform: iOS
    sources:
      - path: ViciInbox
    dependencies:
      - package: TelnyxRTC
        # The package's importable module is TelnyxRTC, but its published SPM
        # product name is "telnyx-webrtc-ios".
        product: telnyx-webrtc-ios
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.vicipeptides.inbox
        PRODUCT_NAME: Vici Inbox
        INFOPLIST_FILE: ViciInbox/Resources/Info.plist
        CODE_SIGN_ENTITLEMENTS: ViciInbox/Resources/ViciInbox.entitlements
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        GENERATE_INFOPLIST_FILE: NO
